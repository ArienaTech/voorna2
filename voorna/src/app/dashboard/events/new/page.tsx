import { CreateEventWizard } from "@/components/dashboard/create-event-wizard";

export const metadata = { title: "Create event — Voorna" };

export default function NewEventPage() {
  return <CreateEventWizard />;
}
